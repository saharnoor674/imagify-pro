# frontend (React)

This folder now contains a minimal Vite + React frontend.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Open the app at the address shown by Vite (usually http://localhost:5173) and ensure the backend is running at http://127.0.0.1:8000.

